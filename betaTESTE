local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

-- ========= CONFIG =========
local velocityMult = 0.5 -- puissance boost basique
local velocityMult2 = 0.4 -- puissance frein
local jumpPower = 50      -- puissance saut
local maxMPH = 1000        -- vitesse max pour speed changer
local accelerationMPH = 300 -- accélération MPH/s

-- Conversion MPH -> studs/s
local function MPHtoStuds(mph)
	return mph / 0.626
end

-- ========= BINDS (MANETTE & CLAVIER) =========
local binds = {
	-- Manette
	boost = Enum.KeyCode.ButtonA,
	accel = Enum.KeyCode.ButtonR2,
	brake = Enum.KeyCode.ButtonB,
	stop  = Enum.KeyCode.ButtonX,
	jump  = Enum.KeyCode.ButtonY,
	flyBoost = Enum.KeyCode.ButtonL1,
	
	-- Clavier (PC)
	pc_boost = Enum.KeyCode.LeftShift,
	pc_accel = Enum.KeyCode.Z,
	pc_brake = Enum.KeyCode.S,
	pc_stop  = Enum.KeyCode.X,
	pc_jump  = Enum.KeyCode.Space,
	pc_flyBoost = Enum.KeyCode.A
}

-- ========= FONCTIONS =========
local function GetSeat()
	local char = LocalPlayer.Character
	if not char then return end
	local hum = char:FindFirstChildWhichIsA("Humanoid")
	if hum then return hum.SeatPart end
end

-- 🔹 BOOST BASIQUE (A / Shift)
local function Boost()
	local seat = GetSeat()
	if seat and seat:IsA("VehicleSeat") then
		seat.AssemblyLinearVelocity += seat.CFrame.LookVector * (velocityMult * 120)
	end
end

-- 🔹 FREIN / STOP / JUMP
local accelerating = false

local function Brake()
	accelerating = false
	local seat = GetSeat()
	if seat and seat:IsA("VehicleSeat") then
		seat.AssemblyLinearVelocity *= (1 - velocityMult2)
	end
end

local function Stop()
	accelerating = false
	local seat = GetSeat()
	if seat and seat:IsA("VehicleSeat") then
		seat.AssemblyLinearVelocity = Vector3.zero
		seat.AssemblyAngularVelocity = Vector3.zero
	end
end

local function Jump()
	local seat = GetSeat()
	if seat and seat:IsA("VehicleSeat") then
		seat.AssemblyLinearVelocity += Vector3.new(0, jumpPower, 0)
	end
end

-- 🔹 ACCÉLÉRATION PROGRESSIVE / SPEED CHANGER
local realisticMode = false

RunService.Heartbeat:Connect(function(dt)
	local seat = GetSeat()
	if not seat or not seat:IsA("VehicleSeat") then return end

	if accelerating then
		if realisticMode then
			local forward = seat.CFrame.LookVector
			local vel = seat.AssemblyLinearVelocity
			seat.AssemblyLinearVelocity = vel + forward * (accelerationMPH * dt / 2)
		else
			local targetSpeed = MPHtoStuds(maxMPH)
			local accelStuds = MPHtoStuds(accelerationMPH)
			local forward = seat.CFrame.LookVector
			local vel = seat.AssemblyLinearVelocity
			local currentSpeed = vel:Dot(forward)
			local newSpeed = math.min(currentSpeed + accelStuds * dt, targetSpeed)
			seat.AssemblyLinearVelocity = Vector3.new(
				forward.X * newSpeed,
				vel.Y,
				forward.Z * newSpeed
			)
		end
	end
end)

-- ========= RAYFIELD =========
local Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()

local Window = Rayfield:CreateWindow({
	Name = "SHIDOOOO",
	LoadingTitle = "Chargement...",
	LoadingSubtitle = "by Shido",
	ConfigurationSaving = { Enabled = true, FolderName = "ShidoMenu", FileName = "Config" },
	KeySystem = false
})

-- ========= PAGES =========
local BoostPage = Window:CreateTab("Boost", 4483362458)
local BrakePage = Window:CreateTab("Frein", 4483362458)
local JumpPage = Window:CreateTab("Jump", 4483362458)
local SpeedPage = Window:CreateTab("Speed", 4483362458)

-- ========= SLIDERS =========
BoostPage:CreateSlider({
	Name = "Vehicule Boost",
	Range = {0,100},
	Increment = 1,
	Suffix = "%",
	CurrentValue = velocityMult*100,
	Callback = function(v) velocityMult = v/100 end
})

BrakePage:CreateSlider({
	Name = "brake power",
	Range = {0,100},
	Increment = 1,
	Suffix = "%",
	CurrentValue = velocityMult2*100,
	Callback = function(v) velocityMult2 = v/100 end
})

JumpPage:CreateSlider({
	Name = "vehicule jump",
	Range = {0,200},
	Increment = 1,
	Suffix = " stud",
	CurrentValue = jumpPower,
	Callback = function(v) jumpPower = v end
})

SpeedPage:CreateSlider({
	Name = "Max vehicule speed",
	Range = {100,2000},
	Increment = 50,
	Suffix = " MPH",
	CurrentValue = maxMPH,
	Callback = function(v) maxMPH = v end
})

SpeedPage:CreateSlider({
	Name = "vrum vrum",
	Range = {50,1000},
	Increment = 50,
	Suffix = " MPH/s",
	CurrentValue = accelerationMPH,
	Callback = function(v) accelerationMPH = v end
})

SpeedPage:CreateToggle({
	Name = "Mode Réaliste",
	CurrentValue = false,
	Callback = function(value) realisticMode = value end
})

-- ========= REBIND SYSTEM (MANETTE & PC) =========
local waitingBind = nil
local rebindButtons = {}

local function startBind(action)
	waitingBind = action
	Rayfield:Notify({
		Title = "Rebind",
		Content = "Appuie sur une touche (Clavier ou Manette)",
		Duration = 3
	})
end

-- Fonctions utilitaires pour afficher proprement les touches combinées
local function getBindText(action, keyGamepad, keyPC)
	return "Changer " .. action .. " (" .. keyGamepad.Name .. " / " .. keyPC.Name .. ")"
end

rebindButtons.boost = BoostPage:CreateButton({
	Name = getBindText("Boost", binds.boost, binds.pc_boost),
	Callback = function() startBind("boost") end
})

rebindButtons.brake = BrakePage:CreateButton({
	Name = getBindText("Frein", binds.brake, binds.pc_brake),
	Callback = function() startBind("brake") end
})

rebindButtons.stop = BrakePage:CreateButton({
	Name = getBindText("Stop", binds.stop, binds.pc_stop),
	Callback = function() startBind("stop") end
})

rebindButtons.jump = JumpPage:CreateButton({
	Name = getBindText("Jump", binds.jump, binds.pc_jump),
	Callback = function() startBind("jump") end
})

rebindButtons.accel = SpeedPage:CreateButton({
	Name = getBindText("Accélération", binds.accel, binds.pc_accel),
	Callback = function() startBind("accel") end
})

-- ========= INPUTS (MANETTE & PC) =========
UserInputService.InputBegan:Connect(function(input, processed)
	if processed then return end

	-- Logique du Rebind automatique (détecte si c'est clavier ou manette)
	if waitingBind then
		if input.UserInputType == Enum.UserInputType.Gamepad1 then
			binds[waitingBind] = input.KeyCode
		elseif input.UserInputType == Enum.UserInputType.Keyboard then
			binds["pc_" .. waitingBind] = input.KeyCode
		else
			return
		end
		
		-- Rafraîchissement du texte du bouton
		if rebindButtons[waitingBind] then
			local cleanName = waitingBind:sub(1,1):upper() .. waitingBind:sub(2)
			rebindButtons[waitingBind]:Set(getBindText(cleanName, binds[waitingBind], binds["pc_" .. waitingBind]))
		end
		waitingBind = nil
		Rayfield:Notify({Title="Rebind", Content="Touche configurée !", Duration=2})
		return
	end

	-- Détection des touches Manette OU Clavier
	local isGamepad = (input.UserInputType == Enum.UserInputType.Gamepad1)
	local isKeyboard = (input.UserInputType == Enum.UserInputType.Keyboard)

	if (isGamepad and input.KeyCode == binds.boost) or (isKeyboard and input.KeyCode == binds.pc_boost) then 
		Boost()
	elseif (isGamepad and input.KeyCode == binds.accel) or (isKeyboard and input.KeyCode == binds.pc_accel) then 
		accelerating = true
	elseif (isGamepad and input.KeyCode == binds.brake) or (isKeyboard and input.KeyCode == binds.pc_brake) then 
		Brake()
	elseif (isGamepad and input.KeyCode == binds.stop) or (isKeyboard and input.KeyCode == binds.pc_stop) then 
		Stop()
	elseif (isGamepad and input.KeyCode == binds.jump) or (isKeyboard and input.KeyCode == binds.pc_jump) then 
		Jump()
	end
end)

UserInputService.InputEnded:Connect(function(input)
	local isGamepad = (input.UserInputType == Enum.UserInputType.Gamepad1)
	local isKeyboard = (input.UserInputType == Enum.UserInputType.Keyboard)

	if (isGamepad and input.KeyCode == binds.accel) or (isKeyboard and input.KeyCode == binds.pc_accel) then 
		accelerating = false 
	end
end)

-- ========= BOOST DIRECTIONNEL (CAMERA FLY BOOST + LIFT) =========

-- CONFIG
local flyBoostPower = 180
local flyBoosting = false
local keepVehicleUpright = true
local liftForce = 65 
local originalPhysicalProps = nil

-- CAMERA
local Camera = workspace.CurrentCamera

-- STABILISATION
local flyGyro = nil

local function StartFlyBoost()
	local seat = GetSeat()
	if not seat or not seat:IsA("VehicleSeat") then return end

	flyBoosting = true

	if not originalPhysicalProps then
		originalPhysicalProps = seat.CustomPhysicalProperties
	end

	if originalPhysicalProps then
		seat.CustomPhysicalProperties = PhysicalProperties.new(
			originalPhysicalProps.Density,
			0, 
			originalPhysicalProps.Elasticity,
			originalPhysicalProps.FrictionWeight,
			originalPhysicalProps.ElasticityWeight
		)
	end

	if keepVehicleUpright and not flyGyro then
		flyGyro = Instance.new("BodyGyro")
		flyGyro.MaxTorque = Vector3.new(1e7,1e7,1e7)
		flyGyro.P = 6000
		flyGyro.D = 400
		flyGyro.CFrame = CFrame.new(seat.Position) * CFrame.Angles(0, math.rad(seat.Orientation.Y), 0)
		flyGyro.Parent = seat
	end

	seat.AssemblyLinearVelocity += Vector3.new(0, liftForce, 0)
end

local function StopFlyBoost()
	flyBoosting = false

	local seat = GetSeat()
	if seat and originalPhysicalProps then
		seat.CustomPhysicalProperties = originalPhysicalProps
	end

	if flyGyro then
		flyGyro:Destroy()
		flyGyro = nil
	end
end

-- APPLICATION DU BOOST DIRECTIONNEL
RunService.Heartbeat:Connect(function(dt)
	if not flyBoosting then return end

	local seat = GetSeat()
	if not seat or not seat:IsA("VehicleSeat") then return end

	local camDir = Camera.CFrame.LookVector
	seat.AssemblyLinearVelocity += camDir * flyBoostPower * dt
	seat.AssemblyLinearVelocity += Vector3.new(0, liftForce * 0.15 * dt, 0)

	if keepVehicleUpright and flyGyro then
		flyGyro.CFrame = CFrame.new(seat.Position) * CFrame.Angles(0, math.rad(seat.Orientation.Y), 0)
	end
end)

-- ========= PAGE RAYFIELD =========
local DirBoostPage = Window:CreateTab("Boost Directionnel", 4483362458)

DirBoostPage:CreateSlider({
	Name = "fly boost (directionnel)",
	Range = {50,500},
	Increment = 10,
	Suffix = " Force",
	CurrentValue = flyBoostPower,
	Callback = function(v) flyBoostPower = v end
})

DirBoostPage:CreateToggle({
	Name = "Véhicule toujours droit",
	CurrentValue = true,
	Callback = function(v)
		keepVehicleUpright = v
		if not v and flyGyro then
			flyGyro:Destroy()
			flyGyro = nil
		end
	end
})

-- ========= REBIND MANETTE / PC =========
rebindButtons.flyBoost = DirBoostPage:CreateButton({
	Name = getBindText("Boost Directionnel", binds.flyBoost, binds.pc_flyBoost),
	Callback = function() startBind("flyBoost") end
})

-- ========= INPUT MAPPER POUR FLY =========
UserInputService.InputBegan:Connect(function(input, gp)
	if gp then return end
	
	local isGamepad = (input.UserInputType == Enum.UserInputType.Gamepad1)
	local isKeyboard = (input.UserInputType == Enum.UserInputType.Keyboard)

	if (isGamepad and input.KeyCode == binds.flyBoost) or (isKeyboard and input.KeyCode == binds.pc_flyBoost) then
		StartFlyBoost()
	end
end)

UserInputService.InputEnded:Connect(function(input)
	local isGamepad = (input.UserInputType == Enum.UserInputType.Gamepad1)
	local isKeyboard = (input.UserInputType == Enum.UserInputType.Keyboard)

	if (isGamepad and input.KeyCode == binds.flyBoost) or (isKeyboard and input.KeyCode == binds.pc_flyBoost) then
		StopFlyBoost()
	end
end)
-- ========= SERVICES REQUIS =========
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera

-- ========= EXTENSION : ESP JOUEURS (ADAPTATION RAYFIELD) =========

local State = {
	ESP = {
		Enabled = true,
		Glow = true,
		Name = true,
		Health = true,
		Distance = true,
		Range = 500000,
		Color = Color3.fromRGB(255,170,60), -- Couleur par défaut (ex: pour la LookLine)
		Transparency = 0.7,
		ShowLookLine = true,
		LookLineLength = 15,
		-- Couleurs automatiques (Totalement invisible pour le joueur, calculé en arrière-plan)
		VisibleColor = Color3.fromRGB(0, 255, 0), -- VERT si tu peux le voir
		HiddenColor = Color3.fromRGB(255, 0, 0)   -- ROUGE si cache derrière un mur
	},
	TrackedCharacters = {}
}

local Config = {
	LookLineDistance = 1.5
}

local function cleanupESP(character)
	local data = State.TrackedCharacters[character]
	if data and data.Visuals then
		data.Visuals:Destroy()
	end
	State.TrackedCharacters[character] = nil
end

-- Cette fonction calcule mathématiquement si un mur bloque la vue (sans rien afficher)
local function checkVisibility(targetCharacter, targetRoot)
	if not LocalPlayer.Character or not Camera then return false end
	
	local rayOrigin = Camera.CFrame.Position
	local rayDirection = targetRoot.Position - rayOrigin
	
	local raycastParams = RaycastParams.new()
	raycastParams.FilterDescendantsInstances = {LocalPlayer.Character, targetCharacter}
	raycastParams.FilterType = Enum.RaycastFilterType.Exclude
	raycastParams.IgnoreWater = true
	
	local raycastResult = Workspace:Raycast(rayOrigin, rayDirection, raycastParams)
	
	-- Si aucun mur n'est touché = Visible
	if not raycastResult then
		return true
	end
	
	return false
end

local function createVisualForCharacter(character, player)
	if not character or character:FindFirstChild("ESPVisual") then return end

	local folder = Instance.new("Folder")
	folder.Name = "ESPVisual"
	folder.Parent = character

	local highlight = Instance.new("Highlight")
	highlight.Parent = folder
	highlight.Name = "Highlight"
	highlight.Adornee = character
	highlight.FillColor = State.ESP.Color
	highlight.OutlineColor = State.ESP.Color
	highlight.FillTransparency = State.ESP.Transparency
	highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop

	local billboard = Instance.new("BillboardGui")
	billboard.Parent = folder
	billboard.Name = "ESPBillboard"
	billboard.Size = UDim2.new(0,200,0,80)
	billboard.StudsOffset = Vector3.new(0,3,0)
	billboard.AlwaysOnTop = true

	local frame = Instance.new("Frame")
	frame.Parent = billboard
	frame.Size = UDim2.fromScale(1,1)
	frame.BackgroundTransparency = 1

	local nameLabel = Instance.new("TextLabel")
	nameLabel.Parent = frame
	nameLabel.Size = UDim2.new(1,0,0.33,0)
	nameLabel.BackgroundTransparency = 1
	nameLabel.Font = Enum.Font.GothamBold
	nameLabel.TextColor3 = State.ESP.Color
	nameLabel.TextStrokeTransparency = 0
	nameLabel.Text = player.Name

	local healthLabel = Instance.new("TextLabel")
	healthLabel.Parent = frame
	healthLabel.Position = UDim2.new(0,0,0.33,0)
	healthLabel.Size = UDim2.new(1,0,0.33,0)
	healthLabel.BackgroundTransparency = 1
	healthLabel.Font = Enum.Font.Gotham
	healthLabel.TextStrokeTransparency = 0

	local distanceLabel = Instance.new("TextLabel")
	distanceLabel.Parent = frame
	distanceLabel.Position = UDim2.new(0,0,0.66,0)
	distanceLabel.Size = UDim2.new(1,0,0.34,0)
	distanceLabel.BackgroundTransparency = 1
	distanceLabel.Font = Enum.Font.Gotham
	distanceLabel.TextStrokeTransparency = 0

	local lookLineFolder = Instance.new("Folder")
	lookLineFolder.Parent = folder
	lookLineFolder.Name = "LookLine"

	for i = 1, State.ESP.LookLineLength do
		local part = Instance.new("Part")
		part.Parent = lookLineFolder
		part.Name = "LookLinePart"..i
		part.Anchored = true
		part.CanCollide = false
		part.CanTouch = false
		part.CanQuery = false
		part.Material = Enum.Material.Neon
		part.Shape = Enum.PartType.Ball
		part.Size = Vector3.new(0.15,0.15,0.15)
		part.Color = State.ESP.Color
	end

	State.TrackedCharacters[character] = {
		Player = player,
		Visuals = folder,
		Highlight = highlight,
		Billboard = billboard,
		NameLabel = nameLabel,
		HealthLabel = healthLabel,
		DistanceLabel = distanceLabel,
		LookLineFolder = lookLineFolder
	}
end

local function updateLookLine(character, lookLineFolder)
	if not State.ESP.ShowLookLine or not State.ESP.Enabled then
		for _, part in ipairs(lookLineFolder:GetChildren()) do
			if part:IsA("BasePart") then part.Transparency = 1 end
		end
		return
	end

	local head = character:FindFirstChild("Head")
	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not head or not humanoid then return end

	local rootPart = character:FindFirstChild("HumanoidRootPart")
	local lookVector = (humanoid.MoveDirection.Magnitude > 0.1 and humanoid.MoveDirection.Unit) 
		or (rootPart and rootPart.CFrame.LookVector) 
		or head.CFrame.LookVector

	for i, part in ipairs(lookLineFolder:GetChildren()) do
		if part:IsA("BasePart") then
			local distance = i * Config.LookLineDistance
			part.Position = head.Position + (lookVector * distance)
			local fade = (i - 1) / math.max(State.ESP.LookLineLength - 1, 1)
			part.Transparency = 0.3 + (fade * 0.7)
			local scale = 1 - (fade * 0.5)
			part.Size = Vector3.new(0.15 * scale, 0.15 * scale, 0.15 * scale)
			part.Color = State.ESP.Color -- Reste sur la couleur fixe définie dans l'UI
		end
	end
end

local function updateVisual(character)
	local data = State.TrackedCharacters[character]
	if not data or not data.Player then return end

	local root = character:FindFirstChild("HumanoidRootPart") or character:FindFirstChild("Head")
	
	-- Détermination de la couleur (Vert si visible, Rouge si caché)
	local statusColor = State.ESP.Color
	if State.ESP.Enabled and root then
		local isVisible = checkVisibility(character, root)
		statusColor = isVisible and State.ESP.VisibleColor or State.ESP.HiddenColor
	end

	if not State.ESP.Enabled then
		data.Highlight.Enabled = false
		data.Billboard.Enabled = false
		updateLookLine(character, data.LookLineFolder)
		return
	end

	if not root then return end

	local myCharacter = LocalPlayer.Character
	local myRoot = myCharacter and (myCharacter:FindFirstChild("HumanoidRootPart") or myCharacter:FindFirstChild("Head"))
	local distance = 0

	if myRoot then
		distance = (root.Position - myRoot.Position).Magnitude
		if distance > State.ESP.Range then
			data.Highlight.Enabled = false
			data.Billboard.Enabled = false
			return
		end
	end

	-- Application des couleurs dynamiques (Uniquement visuel ESP)
	data.Highlight.Enabled = State.ESP.Glow
	data.Highlight.FillColor = statusColor
	data.Highlight.OutlineColor = statusColor
	data.Highlight.FillTransparency = State.ESP.Transparency

	data.Billboard.Enabled = true
	data.Billboard.Adornee = root

	data.NameLabel.Visible = State.ESP.Name
	data.NameLabel.Text = data.Player.Name
	data.NameLabel.TextColor3 = statusColor

	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if humanoid then
		local hpPercent = humanoid.Health / math.max(humanoid.MaxHealth, 1)
		data.HealthLabel.Visible = State.ESP.Health
		data.HealthLabel.Text = "HP: " .. math.floor(humanoid.Health) .. "/" .. math.floor(humanoid.MaxHealth)
		data.HealthLabel.TextColor3 = Color3.new(1 - hpPercent, hpPercent, 0) -- Barre de vie classique (Vert -> Rouge)
	else
		data.HealthLabel.Visible = false
	end

	data.DistanceLabel.Visible = State.ESP.Distance
	data.DistanceLabel.Text = math.floor(distance) .. "m"
	data.DistanceLabel.TextColor3 = statusColor

	updateLookLine(character, data.LookLineFolder)
end

local function onCharacterAdded(character, player)
	if player == LocalPlayer then return end
	local humanoid = character:WaitForChild("Humanoid", 5)
	if not humanoid then return end
	task.wait(0.1)
	createVisualForCharacter(character, player)
end

local function onCharacterRemoving(character)
	cleanupESP(character)
end

-- Connexions Joueurs
for _, player in ipairs(Players:GetPlayers()) do
	if player ~= LocalPlayer then
		if player.Character then onCharacterAdded(player.Character, player) end
		player.CharacterAdded:Connect(function(char) onCharacterAdded(char, player) end)
		player.CharacterRemoving:Connect(onCharacterRemoving)
	end
end

Players.PlayerAdded:Connect(function(player)
	if player == LocalPlayer then return end
	player.CharacterAdded:Connect(function(char) onCharacterAdded(char, player) end)
	player.CharacterRemoving:Connect(onCharacterRemoving)
	if player.Character then onCharacterAdded(player.Character, player) end
end)

Players.PlayerRemoving:Connect(function(player)
	if player.Character then cleanupESP(player.Character) end
end)

RunService.Heartbeat:Connect(function()
	for character, data in pairs(State.TrackedCharacters) do
		if character and character.Parent then
			updateVisual(character)
		else
			cleanupESP(character)
		end
	end
end)

-- ========= PAGE RAYFIELD : ESP JOUEURS =========
local ESPPage = Window:CreateTab("ESP Joueurs", 4483362458)

ESPPage:CreateToggle({
	Name = "ESP",
	CurrentValue = State.ESP.Enabled,
	Callback = function(v) State.ESP.Enabled = v end
})

ESPPage:CreateToggle({
	Name = "Effet de Brillance (Glow)",
	CurrentValue = State.ESP.Glow,
	Callback = function(v) State.ESP.Glow = v end
})

ESPPage:CreateToggle({
	Name = "Show name",
	CurrentValue = State.ESP.Name,
	Callback = function(v) State.ESP.Name = v end
})

ESPPage:CreateToggle({
	Name = "Show life",
	CurrentValue = State.ESP.Health,
	Callback = function(v) State.ESP.Health = v end
})

ESPPage:CreateToggle({
	Name = "Show range",
	CurrentValue = State.ESP.Distance,
	Callback = function(v) State.ESP.Distance = v end
})

ESPPage:CreateToggle({
	Name = "Look Line",
	CurrentValue = State.ESP.ShowLookLine,
	Callback = function(v) State.ESP.ShowLookLine = v end
})

ESPPage:CreateSlider({
	Name = "range ESP",
	Range = {100, 99999999},
	Increment = 100,
	Suffix = "m",
	CurrentValue = State.ESP.Range,
	Callback = function(v) State.ESP.Range = v end
})

ESPPage:CreateSlider({
	Name = "Transparence Remplissage",
	Range = {0, 100},
	Increment = 5,
	Suffix = "%",
	CurrentValue = State.ESP.Transparency * 100,
	Callback = function(v) State.ESP.Transparency = v / 100 end
})

ESPPage:CreateColorPicker({
	Name = "Couleur LookLine",
	Color = State.ESP.Color,
	Callback = function(v) State.ESP.Color = v end
})
-- ========= EXTENSION : VEHICLE FLY (ADAPTATION RAYFIELD) =========

-- Variables Globales de l'Extension
local velocityHandlerName = "VehicleFlyVelocity32"
local gyroHandlerName = "VehicleFlyGyro64"
local VelocityHandler = nil
local GyroHandler = nil

local FlyEnabled = false
local flightWithoutFlyEnabled = false
local isAntiLockOn = false
local isPitchOn = false

local seatConnection = nil
local unseatConnection = nil
local flyLoopConnection = nil

local flySpeedMultiplier = 1
local forwardVelocity = 100
local leftVelocity = 100
local rightVelocity = 100
local upVelocity = 50
local downVelocity = 50
local rotationSpeed = 5

-- Table pour simuler les directions actives (gérées par clavier/clics)
local flyMovementDirection = {}

-- S'assurer que le joueur est chargé
local flyPlayer = game:GetService("Players").LocalPlayer

-- Nettoyage des instances de vol
local function DisableFlight()
	if VelocityHandler then VelocityHandler:Destroy() VelocityHandler = nil end
	if GyroHandler then GyroHandler:Destroy() GyroHandler = nil end
end

-- Création des instances de vol physiques
local function setupFlyInstances(character)
	if not character or not character:FindFirstChild("HumanoidRootPart") then return end
	
	local hrp = character.HumanoidRootPart
	local existingVelocity = hrp:FindFirstChild(velocityHandlerName)
	local existingGyro = hrp:FindFirstChild(gyroHandlerName)
	
	if existingVelocity then existingVelocity:Destroy() end
	if existingGyro then existingGyro:Destroy() end
	
	VelocityHandler = Instance.new("BodyVelocity")
	VelocityHandler.Name = velocityHandlerName
	VelocityHandler.Parent = hrp
	VelocityHandler.MaxForce = Vector3.new(9e9, 9e9, 9e9)
	VelocityHandler.Velocity = Vector3.new()
	
	GyroHandler = Instance.new("BodyGyro")
	GyroHandler.Name = gyroHandlerName
	GyroHandler.Parent = hrp
	GyroHandler.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
	GyroHandler.P = 1000
	GyroHandler.D = 50
	GyroHandler.CFrame = hrp.CFrame
end

local function EnableFlight()
	local character = flyPlayer.Character
	if character then setupFlyInstances(character) end
end

-- Gestionnaire de mouvements physiques (Adapté de ton handleInput d'origine)
local function handleFlyInput()
	local character = flyPlayer.Character
	if not character then return end
	
	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if not humanoid then return end
	
	if FlyEnabled and (humanoid.SeatPart or flightWithoutFlyEnabled) then
		local rootPart = character:FindFirstChild("HumanoidRootPart")
		if not rootPart or not VelocityHandler then return end
		
		local gyro = rootPart:FindFirstChild(gyroHandlerName)
		local speed = flySpeedMultiplier
		local velocity = Vector3.new()
		
		local function getMovementCFrame()
			if isPitchOn and gyro then
				return gyro.CFrame
			elseif gyro then
				local lookVector = gyro.CFrame.LookVector
				return CFrame.new(gyro.Parent.Position, gyro.Parent.Position + Vector3.new(lookVector.X, 0, lookVector.Z))
			end
			return rootPart.CFrame
		end
		
		local movementCFrame = getMovementCFrame()
		
		-- Lecture des directions via ZQSD ou les touches virtuelles
		for _, direction in ipairs(flyMovementDirection) do
			if direction == "forward" then
				velocity += movementCFrame.LookVector * forwardVelocity
			elseif direction == "backward" then
				velocity += -movementCFrame.LookVector * forwardVelocity
			elseif direction == "left" then
				velocity += movementCFrame.RightVector * -leftVelocity
			elseif direction == "right" then
				velocity += movementCFrame.RightVector * rightVelocity
			elseif direction == "up" then
				velocity += Vector3.new(0, upVelocity, 0)
			elseif direction == "down" then
				velocity += Vector3.new(0, -downVelocity, 0)
			elseif direction == "rotateLeft" and gyro then
				gyro.CFrame = gyro.CFrame * CFrame.Angles(0, math.rad(-rotationSpeed), 0)
			elseif direction == "rotateRight" and gyro then
				gyro.CFrame = gyro.CFrame * CFrame.Angles(0, math.rad(rotationSpeed), 0)
			end
		end
		
		-- Intégration du ControlModule d'origine pour les joysticks/touches clavier natives
		local thumbstickDirection = Vector2.new(0, 0)
		pcall(function()
			local controlModule = require(flyPlayer.PlayerScripts:WaitForChild("PlayerModule"):WaitForChild("ControlModule"))
			thumbstickDirection = controlModule:GetMoveVector()
		end)
		
		if #flyMovementDirection > 0 or thumbstickDirection.Magnitude > 0 then
			local combinedVelocity = velocity + (
				movementCFrame.RightVector * thumbstickDirection.X * forwardVelocity * 2 +
				movementCFrame.LookVector * -thumbstickDirection.Z * forwardVelocity * 2
			)
			
			if isAntiLockOn and humanoid.SeatPart then
				local seatForward = humanoid.SeatPart.CFrame.LookVector
				local seatRight = humanoid.SeatPart.CFrame.RightVector
				local seatUp = humanoid.SeatPart.CFrame.UpVector
				local seatVelocity = Vector3.new()
				
				for _, direction in ipairs(flyMovementDirection) do
					if direction == "forward" then seatVelocity += seatForward * forwardVelocity
					elseif direction == "backward" then seatVelocity += -seatForward * forwardVelocity
					elseif direction == "left" then seatVelocity += -seatRight * leftVelocity
					elseif direction == "right" then seatVelocity += seatRight * rightVelocity
					elseif direction == "up" then seatVelocity += seatUp * upVelocity
					elseif direction == "down" then seatVelocity += -seatUp * downVelocity
					end
				end
				
				VelocityHandler.Velocity = seatVelocity * speed
			else
				VelocityHandler.Velocity = combinedVelocity * speed
			end
		else
			VelocityHandler.Velocity = Vector3.new()
		end
		
		-- Alignement Caméra si Anti-Lock désactivé
		if not isAntiLockOn then
			local camera = workspace.CurrentCamera
			if isPitchOn and gyro then
				gyro.CFrame = camera.CFrame
			elseif gyro then
				local flatLookVector = camera.CFrame.LookVector * Vector3.new(1, 0, 1)
				gyro.CFrame = CFrame.new(gyro.Parent.Position, gyro.Parent.Position + flatLookVector)
			end
		end
	else
		if VelocityHandler then VelocityHandler.Velocity = Vector3.new() end
	end
end

-- Démarrage de la boucle RenderStepped pour la réactivité physique
flyLoopConnection = game:GetService("RunService").RenderStepped:Connect(handleFlyInput)

-- ========= CONFIGURATION DE L'ONGLET RAYFIELD =========
local FlyTab = Window:CreateTab("Vehicle Fly", 4483362458)

local FlyToggle = FlyTab:CreateToggle({
	Name = "vehicule fly",
	CurrentValue = false,
	Callback = function(state)
		FlyEnabled = state
		local character = flyPlayer.Character
		local humanoid = character and character:FindFirstChildOfClass("Humanoid")
		
		if FlyEnabled then
			if flightWithoutFlyEnabled then
				EnableFlight()
			elseif humanoid then
				if seatConnection then seatConnection:Disconnect() end
				if unseatConnection then unseatConnection:Disconnect() end
				
				if humanoid.SeatPart then
					EnableFlight()
				else
					seatConnection = humanoid:GetPropertyChangedSignal("SeatPart"):Connect(function()
						if humanoid.SeatPart then EnableFlight() end
					end)
				end
				
				unseatConnection = humanoid:GetPropertyChangedSignal("SeatPart"):Connect(function()
					if not humanoid.SeatPart then DisableFlight() end
				end)
			end
		else
			DisableFlight()
			if seatConnection then seatConnection:Disconnect() seatConnection = nil end
			if unseatConnection then unseatConnection:Disconnect() unseatConnection = nil end
		end
	end
})

FlyTab:CreateToggle({
	Name = "FwF simple fly no vehicule",
	CurrentValue = false,
	Callback = function(state)
		flightWithoutFlyEnabled = state
		if FlyEnabled and not flightWithoutFlyEnabled then
			local character = flyPlayer.Character
			local humanoid = character and character:FindFirstChildOfClass("Humanoid")
			if humanoid and not humanoid.SeatPart then
				DisableFlight()
			end
		end
	end
})

FlyTab:CreateSlider({
	Name = "speed vehicule fly",
	Range = {1, 50},
	Increment = 1,
	Suffix = "x",
	CurrentValue = flySpeedMultiplier,
	Callback = function(value)
		flySpeedMultiplier = value
	end
})

local ALToggle
local PitchToggle

ALToggle = FlyTab:CreateToggle({
	Name = "Anti-Lock",
	CurrentValue = false,
	Callback = function(state)
		isAntiLockOn = state
		if isAntiLockOn and isPitchOn then
			isPitchOn = false
			Rayfield:Notify({Title = "Fly Config", Content = "Pitch désactivé automatiquement par l'Anti-Lock", Duration = 2})
		end
	end
})

PitchToggle = FlyTab:CreateToggle({
	Name = "Mode Pitch",
	CurrentValue = false,
	Callback = function(state)
		if isAntiLockOn then
			isPitchOn = false
			Rayfield:Notify({Title = "Erreur", Content = "Désactive d'abord l'Anti-Lock !", Duration = 2})
		else
			isPitchOn = state
		end
	end
})
-- ========= EXTENSION : KEYBIND POUR LE VEHICLE FLY =========

-- Touches par défaut
local flyBinds = {
	pc = Enum.KeyCode.F,
	gamepad = Enum.KeyCode.ButtonL3
}

local waitingFlyBind = false

-- Crée un conteneur pour mettre à jour le texte du bouton de rebind
local flyBindButton = nil

local function getFlyBindText()
	return "Touche d'activation (" .. flyBinds.pc.Name .. " / " .. flyBinds.gamepad.Name .. ")"
end

-- Ajout des éléments dans l'onglet existant "Vehicle Fly"
-- Note : L'onglet 'FlyTab' doit être défini plus haut dans ton script
if FlyTab then
	
	-- Bouton pour changer la touche (Rebind)
	flyBindButton = FlyTab:CreateButton({
		Name = getFlyBindText(),
		Callback = function()
			waitingFlyBind = true
			Rayfield:Notify({
				Title = "Raccourci Fly",
				Content = "Appuie sur une touche du clavier ou un bouton de manette...",
				Duration = 3
			})
		end
	})
	
	-- Détection de l'appui sur la touche pour On/Off
	UserInputService.InputBegan:Connect(function(input, processed)
		if processed then return end
		
		-- Gestion du rebind si on attend une touche
		if waitingFlyBind then
			if input.UserInputType == Enum.UserInputType.Keyboard then
				flyBinds.pc = input.KeyCode
				waitingFlyBind = false
			elseif input.UserInputType == Enum.UserInputType.Gamepad1 then
				flyBinds.gamepad = input.KeyCode
				waitingFlyBind = false
			end
			
			if not waitingFlyBind then
				flyBindButton:Set(getFlyBindText())
				Rayfield:Notify({
					Title = "Raccourci Fly",
					Content = "Nouvelle touche enregistrée !",
					Duration = 2
				})
			end
			return
		end
		
		-- Activation / Désactivation du Fly via la touche
		local isPCKey = (input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode == flyBinds.pc)
		local isGamepadKey = (input.UserInputType == Enum.UserInputType.Gamepad1 and input.KeyCode == flyBinds.gamepad)
		
		if isPCKey or isGamepadKey then
			-- On change l'état du FlyToggle (ce qui déclenche automatiquement la logique du Fly)
			if FlyToggle then
				FlyToggle:Set(not FlyEnabled)
			end
		end
	end)
end
-- ========= RECONSTRUCTION : SYSTEME NOCLIP TOTAL (VÉHICULE + JOUEUR) =========

local wallNoclipEnabled = false
local noclipConnection = nil
local originalCollides = {}

-- Fonction pour passer en noclip le véhicule ET le joueur
local function applyTotalNoclip()
	-- 1. Noclip du Véhicule
	local seat = GetSeat()
	if seat and seat:IsA("VehicleSeat") then
		local vehicleModel = seat:FindFirstAncestorOfClass("Model") or seat.Parent
		if vehicleModel then
			for _, part in ipairs(vehicleModel:GetDescendants()) do
				if part:IsA("BasePart") then
					-- On ignore les pièces du joueur ici pour les traiter juste après
					if not part:IsAncestorOf(LocalPlayer.Character) then
						if originalCollides[part] == nil then
							originalCollides[part] = part.CanCollide
						end
						part.CanCollide = false
					end
				end
			end
		end
		-- Sécurité pour maintenir le moteur de conduite actif
		seat.CanCollide = false
	end

	-- 2. Noclip du Joueur (Évite l'éjection à travers les structures)
	local char = LocalPlayer.Character
	if char then
		for _, part in ipairs(char:GetDescendants()) do
			if part:IsA("BasePart") then
				if originalCollides[part] == nil then
					originalCollides[part] = part.CanCollide
				end
				part.CanCollide = false
			end
		end
	end
end

-- Restauration complète des collisions d'origine
local function disableTotalNoclip()
	for part, originalState in pairs(originalCollides) do
		if part and part.Parent then
			part.CanCollide = originalState
		end
	end
	originalCollides = {}
end

-- ========= NOUVEL ONGLET RAYFIELD : NOCLIP AVEC RACCOURCI =========
local NoclipPage = Window:CreateTab("Noclip", 4483362458)
local NoclipToggle = nil

NoclipToggle = NoclipPage:CreateToggle({
	Name = "vahicule noclip",
	CurrentValue = false,
	Callback = function(value)
		wallNoclipEnabled = value
		
		if wallNoclipEnabled then
			-- Boucle ultra-rapide avant le rendu physique
			noclipConnection = RunService.Stepped:Connect(applyTotalNoclip)
			Rayfield:Notify({Title = "Noclip", Content = "Noclip Global Activé (Voiture + Perso) !", Duration = 2})
		else
			if noclipConnection then
				noclipConnection:Disconnect()
				noclipConnection = nil
			end
			disableTotalNoclip()
			Rayfield:Notify({Title = "Noclip", Content = "UnNoclip", Duration = 2})
		end
	end
})

-- Système de touche (Keybind) juste en dessous
NoclipPage:CreateKeybind({
	Name = "Raccourci Clavier Noclip",
	CurrentKeybind = "N",
	HoldToInteract = false,
	Flag = "NoclipKeybind",
	Callback = function()
		if NoclipToggle then
			NoclipToggle:Set(not wallNoclipEnabled)
		end
	end,
})
-- ========= EXTENSION : ONGLET SAUVEGARDE (ADAPTÉ À SHIDOMENU) =========

local SaveTab = Window:CreateTab("Sauvegarde", 4483362458)

SaveTab:CreateSection("Gestion de la Configuration")

SaveTab:CreateButton({
	Name = "Sauvegarder mes Touches & Options",
	Callback = function()
		-- Sauvegarde les données dans le dossier ShidoMenu/Config
		pcall(function()
			Rayfield:SaveConfiguration()
		end)
		
		Rayfield:Notify({
			Title = "Configuration",
			Content = "Toutes vos touches et options ont été sauvegardées dans ShidoMenu !",
			Duration = 2
		})
	end
})

SaveTab:CreateButton({
	Name = "Charger la dernière sauvegarde",
	Callback = function()
		-- Recharge manuellement les données
		pcall(function()
			Rayfield:LoadConfiguration()
		end)
		
		Rayfield:Notify({
			Title = "Configuration",
			Content = "Configuration rechargée avec succès !",
			Duration = 2
		})
	end
})

SaveTab:CreateSection("Aide & Infos")
SaveTab:CreateParagraph({
	Title = "Comment fonctionne l'automatisation ?", 
	Content = "Le script chargera tes touches automatiquement à chaque fois que tu relanceras le menu. Quand tu modifies un Keybind, clique simplement sur 'Sauvegarder mes Touches & Options' pour mettre à jour ton fichier de configuration."
})

-- ========= CHARGEMENT AUTOMATIQUE AU LANCEMENT =========
-- Restaure instantanément tes touches 1 seconde après l'exécution du script
task.spawn(function()
	task.wait(1)
	pcall(function()
		Rayfield:LoadConfiguration()
	end)
end)
-- ========= EXTENSION : MODE BÉLIER PHYSIQUE (IMPACT FLING) =========

local ramImpactEnabled = false
local ramConnection = nil
local ramPower = 150 -- Puissance de la projection (Augmente si tu veux les envoyer sur la Lune)

local function applyRamImpact()
	local mySeat = GetSeat()
	if not mySeat or not mySeat:IsA("VehicleSeat") then return end

	local myVehicle = mySeat:FindFirstAncestorOfClass("Model") or mySeat.Parent
	if not myVehicle then return end

	-- On cherche les autres véhicules à proximité
	for _, obj in ipairs(workspace:GetDescendants()) do
		if obj:IsA("VehicleSeat") and obj ~= mySeat then
			local otherVehicle = obj:FindFirstAncestorOfClass("Model") or obj.Parent
			
			if otherVehicle and obj.PrimaryPart then
				-- Calcul de la distance entre ma voiture et l'autre
				local distance = (mySeat.Position - obj.Position).Magnitude
				
				-- Si l'autre voiture est à moins de 12 studs (zone d'impact / collision)
				if distance <= 12 then
					-- On cible toutes les pièces physiques de l'autre voiture
					for _, part in ipairs(otherVehicle:GetDescendants()) do
						if part:IsA("BasePart") and not part.Anchored then
							-- On applique une impulsion physique violente (vitesse + rotation)
							part.Velocity = Vector3.new(math.random(-ramPower, ramPower), ramPower * 1.5, math.random(-ramPower, ramPower))
							part.RotVelocity = Vector3.new(ramPower, ramPower, ramPower)
						end
					end
				end
			end
		end
	end
end

-- ========= CRÉATION DE L'ONGLET DÉDIÉ =========
local RamTab = Window:CreateTab("Bélier", 4483362458)

RamTab:CreateSection("Options de Combat")

local RamToggle = nil
RamToggle = RamTab:CreateToggle({
	Name = "Activer le Mode Bélier (Fling au Contact)",
	CurrentValue = false,
	Callback = function(value)
		ramImpactEnabled = value
		
		if ramImpactEnabled then
			-- Boucle rapide pour checker les collisions imminentes
			ramConnection = RunService.Heartbeat:Connect(applyRamImpact)
			Rayfield:Notify({
				Title = "Mode Bélier",
				Content = "Activé ! Touchez un véhicule pour l'envoyer valser.",
				Duration = 2
			})
		else
			if ramConnection then
				ramConnection:Disconnect()
				ramConnection = nil
			end
			Rayfield:Notify({
				Title = "Mode Bélier",
				Content = "Désactivé. Conduite pacifique.",
				Duration = 2
			})
		end
	end
})

-- Raccourci Clavier (Keybind) pour l'activer/désactiver à la volée
RamTab:CreateKeybind({
	Name = "Raccourci Mode Bélier",
	CurrentKeybind = "K", -- Touche par défaut (Modifiable)
	HoldToInteract = false,
	Flag = "RamKeybind",
	Callback = function()
		if RamToggle then
			RamToggle:Set(not ramImpactEnabled)
		end
	end,
})
-- ========== PARAMÈTRES ET CONFIGURATION INITIALE ==========
local player = game.Players.LocalPlayer
local mouse = player:GetMouse()
local camera = game.Workspace.CurrentCamera
local runService = game:GetService("RunService")

local megaSettings = {
    Enabled = false,
    AimKeyMobile = true,
    WallCheck = true, 
    TeamCheck = false,
    IgnoreFriends = false,
    PrioritizeClosest = true,
    PrioritizeLowestHP = false,
    
    TargetPart = "Head",
    Smoothness = 5,
    Prediction = 0.165,
    FOV = 180,
    
    ShowFOV = true,
    RGBFOV = false, 
    FOVColor = Color3.fromRGB(255, 50, 50),
    FOVSides = 64,   
    FOVThickness = 2, 
    
    AutoShoot = false,
    TriggerBot = false,
    Airshot = false,
    Knockback = false
}

-- Création et initialisation de l'objet de dessin du FOV
local fovCircle = Drawing.new("Circle")
fovCircle.Visible = false
fovCircle.Radius = megaSettings.FOV
fovCircle.Thickness = megaSettings.FOVThickness
fovCircle.Position = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
fovCircle.NumSides = megaSettings.FOVSides
fovCircle.Filled = false

-- ========== MOTEUR DE DÉTECTION ET DE VISÉE ==========
local function isTargetVisible(targetPart, targetCharacter)
    local raycastParams = RaycastParams.new()
    raycastParams.FilterDescendantsInstances = {player.Character, camera}
    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    raycastParams.IgnoreWater = true
    
    local startPos = camera.CFrame.Position
    local direction = (targetPart.Position - startPos)
    local raycastResult = workspace:Raycast(startPos, direction, raycastParams)
    
    if not raycastResult then return true end
    if raycastResult.Instance:IsDescendantOf(targetCharacter) then return true end
    return false
end

local function getClosestTarget()
    local closestTarget = nil
    local shortestDistance = megaSettings.FOV
    local lowestHealth = math.huge
    
    local mousePos = Vector2.new(mouse.X, mouse.Y)
    if megaSettings.AimKeyMobile then
        mousePos = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
    end
    
    for _, target in pairs(game.Players:GetPlayers()) do
        if target ~= player and target.Character and target.Character:FindFirstChild("Humanoid") and target.Character.Humanoid.Health > 0 then
            
            if megaSettings.IgnoreFriends and target:IsFriendsWith(player.UserId) then continue end
            if megaSettings.TeamCheck and target.Team == player.Team then continue end
            
            local targetPart
            if megaSettings.TargetPart == "Random" then
                local parts = {"Head", "Torso", "HumanoidRootPart"}
                targetPart = target.Character:FindFirstChild(parts[math.random(1, #parts)])
            else
                targetPart = target.Character:FindFirstChild(megaSettings.TargetPart)
            end
            
            if not targetPart then targetPart = target.Character:FindFirstChild("Head") end
            
            if targetPart then
                local screenPos, onScreen = camera:WorldToScreenPoint(targetPart.Position)
                if onScreen then
                    local distance = (mousePos - Vector2.new(screenPos.X, screenPos.Y)).Magnitude
                    local currentHealth = target.Character.Humanoid.Health
                    
                    if distance < megaSettings.FOV then
                        if megaSettings.WallCheck and not isTargetVisible(targetPart, target.Character) then
                            continue
                        end
                        
                        if megaSettings.PrioritizeLowestHP then
                            if currentHealth < lowestHealth then
                                lowestHealth = currentHealth
                                closestTarget = {Part = targetPart, Player = target, ScreenPos = screenPos}
                            end
                        else
                            if distance < shortestDistance then
                                shortestDistance = distance
                                closestTarget = {Part = targetPart, Player = target, ScreenPos = screenPos}
                            end
                        end
                    end
                end
            end
        end
    end
    return closestTarget
end

-- Boucle de rafraîchissement
local aimbotConnection
aimbotConnection = runService.RenderStepped:Connect(function()
    fovCircle.Radius = megaSettings.FOV
    fovCircle.Visible = megaSettings.ShowFOV
    fovCircle.Thickness = megaSettings.FOVThickness
    fovCircle.NumSides = megaSettings.FOVSides
    fovCircle.Position = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
    
    if megaSettings.RGBFOV then
        local hue = (tick() % 4) / 4
        fovCircle.Color = Color3.fromHSV(hue, 1, 1)
    else
        fovCircle.Color = megaSettings.FOVColor
    end
    
    if megaSettings.Enabled then
        local targetData = getClosestTarget()
        
        if targetData then
            local target = targetData.Part
            local targetPos = target.Position
            
            if megaSettings.Prediction > 0 and target.Parent and target.Parent:FindFirstChild("HumanoidRootPart") then
                local velocity = target.Parent.HumanoidRootPart.Velocity
                targetPos = targetPos + (velocity * megaSettings.Prediction)
            end
            
            if megaSettings.Airshot and target.Parent and target.Parent:FindFirstChild("Humanoid") then
                local humanoid = target.Parent.Humanoid
                if humanoid.FloorMaterial == Enum.Material.Air then
                    targetPos = targetPos + Vector3.new(0, 2, 0)
                end
            end
            
            local targetCFrame = CFrame.new(camera.CFrame.Position, targetPos)
            camera.CFrame = camera.CFrame:Lerp(targetCFrame, 1 / megaSettings.Smoothness)
            
            if megaSettings.AutoShoot then
                local tool = player.Character and player.Character:FindFirstChildOfClass("Tool")
                if tool and tool:FindFirstChild("RemoteFunction") then tool:Activate() end
            end
            
            if megaSettings.TriggerBot then
                local mousePos = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y / 2)
                local distToCenter = (mousePos - Vector2.new(targetData.ScreenPos.X, targetData.ScreenPos.Y)).Magnitude
                if distToCenter < 50 then
                    local tool = player.Character and player.Character:FindFirstChildOfClass("Tool")
                    if tool and tool:FindFirstChild("RemoteFunction") then tool:Activate() end
                end
            end
            
            if megaSettings.Knockback and target.Parent and target.Parent:FindFirstChild("HumanoidRootPart") then
                local root = target.Parent.HumanoidRootPart
                root.Velocity = root.Velocity + (root.Position - camera.CFrame.Position).Unit * 50
            end
        end
    end
end)

-- ========== CRÉATION DE L'ONGLET DANS TON MENU ==========
local AimTab = Window:CreateTab("🎯 Custom Aimbot", 4483362458)

AimTab:CreateSection("Contrôle de Base")

local MainToggle = AimTab:CreateToggle({
    Name = "Activer l'Aimbot",
    CurrentValue = megaSettings.Enabled,
    Callback = function(v) 
        megaSettings.Enabled = v 
    end
})

-- Ajout du Keybind manquant pour l'activation rapide
AimTab:CreateKeybind({
    Name = "Raccourci Activation (Keybind)",
    CurrentKeybind = "E",
    HoldToInteract = false,
    Flag = "ToggleKeyAimbot",
    Callback = function()
        MainToggle:Set(not megaSettings.Enabled)
    end,
})

AimTab:CreateToggle({
    Name = "Mode Mobile (Centrer sur l'écran)",
    CurrentValue = megaSettings.AimKeyMobile,
    Callback = function(v) megaSettings.AimKeyMobile = v end
})

-- SECTION PERSONNALISATION (CORRECTEMENT LIÉE POUR RAYFIELD)
AimTab:CreateSection("🎛️ Personnalisation de la Visée")

AimTab:CreateSlider({
    Name = "Taille / Rayon du FOV",
    Min = 10,
    Max = 800,
    CurrentValue = 180,
    Flag = "AimbotFOVSize",
    Callback = function(Value) 
        megaSettings.FOV = tonumber(Value)
    end
})

AimTab:CreateSlider({
    Name = "Fluidité (Smoothness)",
    Min = 1,
    Max = 30,
    CurrentValue = 5,
    Flag = "AimbotSmooth",
    Callback = function(Value) 
        megaSettings.Smoothness = tonumber(Value)
    end
})

AimTab:CreateSlider({
    Name = "Ajustement de la Prédiction",
    Min = 0,
    Max = 500,
    CurrentValue = 165,
    Flag = "AimbotPredict",
    Callback = function(Value) 
        megaSettings.Prediction = tonumber(Value) / 1000
    end
})

AimTab:CreateDropdown({
    Name = "Partie du Corps à Cibler",
    Options = {"Head", "Torso", "HumanoidRootPart", "Random"},
    CurrentValue = "Head",
    Flag = "AimbotTargetPart",
    Callback = function(v) megaSettings.TargetPart = v end
})

-- AJOUT DES CHOIX DE PRIORITÉ EXCLUSIFS DE TON SCRIPT ORIGINAL
local ClosestToggle, LowHPToggle
AimTab:CreateSection("👑 Préférences de Ciblage")

ClosestToggle = AimTab:CreateToggle({
    Name = "Cible la plus proche du viseur",
    CurrentValue = megaSettings.PrioritizeClosest,
    Callback = function(v)
        if v then
            megaSettings.PrioritizeClosest = true
            megaSettings.PrioritizeLowestHP = false
            if LowHPToggle then LowHPToggle:Set(false) end
        end
    end
})

LowHPToggle = AimTab:CreateToggle({
    Name = "Cible avec le moins de points de vie",
    CurrentValue = megaSettings.PrioritizeLowestHP,
    Callback = function(v)
        if v then
            megaSettings.PrioritizeLowestHP = true
            megaSettings.PrioritizeClosest = false
            if ClosestToggle then ClosestToggle:Set(false) end
        end
    end
})

-- SECTION PERSONNALISATION DU VISUEL (COULEURS / STYLE)
AimTab:CreateSection("🎨 Personnalisation Visuelle du FOV")

AimTab:CreateToggle({
    Name = "Afficher le cercle de FOV",
    CurrentValue = megaSettings.ShowFOV,
    Callback = function(v) megaSettings.ShowFOV = v end
})

AimTab:CreateToggle({
    Name = "Activer l'Effet Rainbow RGB (Arc-en-ciel)",
    CurrentValue = megaSettings.RGBFOV,
    Callback = function(v) megaSettings.RGBFOV = v end
})

AimTab:CreateColorPicker({
    Name = "Changer la Couleur Statique du FOV",
    Color = megaSettings.FOVColor,
    Callback = function(customColor) 
        megaSettings.FOVColor = customColor 
    end
})

AimTab:CreateSlider({
    Name = "Épaisseur du trait du FOV",
    Min = 1,
    Max = 10,
    CurrentValue = 2,
    Flag = "AimbotFOVThickness",
    Callback = function(Value) 
        megaSettings.FOVThickness = tonumber(Value)
    end
})

AimTab:CreateSlider({
    Name = "Qualité de forme du cercle (NumSides)",
    Min = 16,
    Max = 128,
    CurrentValue = 64,
    Flag = "AimbotFOVSides",
    Callback = function(Value) 
        megaSettings.FOVSides = tonumber(Value)
    end
})

-- SECTION SÉCURITÉ ET FILTRES
AimTab:CreateSection("🛡️ Filtres de Sécurité")

AimTab:CreateToggle({Name = "Wall Check (Ne vise pas à travers les murs)", CurrentValue = megaSettings.WallCheck, Callback = function(v) megaSettings.WallCheck = v end})
AimTab:CreateToggle({Name = "Ignorer mon Équipe (Team Check)", CurrentValue = megaSettings.TeamCheck, Callback = function(v) megaSettings.TeamCheck = v end})
AimTab:CreateToggle({Name = "Ignorer ma liste d'Amis", CurrentValue = megaSettings.IgnoreFriends, Callback = function(v) megaSettings.IgnoreFriends = v end})

-- SECTION EXTRAS
AimTab:CreateSection("⚡ Options de Combat Avancées")

AimTab:CreateToggle({Name = "Auto Shoot (Tir Automatique)", CurrentValue = megaSettings.AutoShoot, Callback = function(v) megaSettings.AutoShoot = v end})
AimTab:CreateToggle({Name = "Trigger Bot", CurrentValue = megaSettings.TriggerBot, Callback = function(v) megaSettings.TriggerBot = v end})
AimTab:CreateToggle({Name = "Airshot Assist (Anticipe les sauts)", CurrentValue = megaSettings.Airshot, Callback = function(v) megaSettings.Airshot = v end})
AimTab:CreateToggle({Name = "Knockback Répercuté (Force physique)", CurrentValue = megaSettings.Knockback, Callback = function(v) megaSettings.Knockback = v end})
